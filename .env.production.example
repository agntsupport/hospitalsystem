# ABOUTME: Ejemplo de variables de entorno para producción en EasyPanel
# Copia este archivo a .env y configura tus valores reales

# ========================================
# DATABASE (PostgreSQL)
# ========================================
POSTGRES_DB=hospital_management
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CAMBIA_ESTO_POR_PASSWORD_SEGURO
POSTGRES_PORT=5432

# ========================================
# BACKEND
# ========================================
BACKEND_PORT=3001
NODE_ENV=production

# JWT Secret (CRÍTICO - genera uno nuevo)
# Puedes usar: openssl rand -base64 32
JWT_SECRET=CAMBIA_ESTO_POR_SECRET_SEGURO_DE_32_CARACTERES

# Security
TRUST_PROXY=true

# ========================================
# FRONTEND
# ========================================
FRONTEND_PORT=80
# IMPORTANTE: En EasyPanel, usa la URL de tu backend CON /api al final
# El frontend espera que VITE_API_URL incluya /api
# Ejemplo correcto: https://hospital-api.yourdomain.com/api
VITE_API_URL=https://TU_DOMINIO_BACKEND.easypanel.host/api

# ========================================
# EASYPANEL SPECIFICS
# ========================================
# EasyPanel asigna automáticamente:
# - Dominio para backend: https://[nombre-servicio-backend].easypanel.host
# - Dominio para frontend: https://[nombre-servicio-frontend].easypanel.host
#
# IMPORTANTE: Configura VITE_API_URL con el dominio del backend + /api
# Ejemplo: https://hospital-management-system-app.1nse3e.easypanel.host/api
#
# El backend debe tener configurado CORS para permitir el dominio del frontend
# en server-modular.js línea 68-87
